/**
 * @typedef {'png' | 'jpeg'} ImageFormat
 * Supported output image formats
 */

/**
 * @typedef {Object} ConvertOptions
 * Options for image conversion
 * @property {number} [quality] - Quality for JPEG conversion (0-1), default: 0.9
 * @property {number} [maxWidth] - Maximum width for the output image
 * @property {number} [maxHeight] - Maximum height for the output image
 * @property {boolean} [maintainAspectRatio=true] - Whether to maintain aspect ratio when resizing
 */

/**
 * @typedef {Object} ImageDimensions
 * Image dimensions
 * @property {number} width - Image width in pixels
 * @property {number} height - Image height in pixels
 */

// This file only contains type definitions for JSDoc
// No exports needed
